import { MealInterface } from "../../entities/Meal";


/**
 * Data Transfer Object (DTO) representing the request to create a new meal.
 *
 * @interface
 */
export interface ICreateMealRequestDTO extends MealInterface{
}
