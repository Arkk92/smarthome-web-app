/**
 * Enum representing error types related to week validation.
 *
 * @enum
 */
export enum WeekErrorType {
    /**
     * Error type indicating that the provided week date is invalid.
     */
    InvalidWeek = 'Invalid Week Period',
  }