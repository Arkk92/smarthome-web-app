/**
 * Enum representing success types related to ingridient operations.
 *
 * @enum
 */
export enum IngridientSuccessType {
    /**
     * Success type indicating that the ingridient was deleted successfully.
     */
    IngridientDeleted = 'Ingridient deleted with success!',
  }