/**
 * Enum representing success types related to week schedule operations.
 *
 * @enum
 */
export enum WeekScheduleSuccessType {
    /**
     * Success type indicating that the week schedule was deleted successfully.
     */
    WeekScheduleDeleted = 'WeekSchedule deleted with success!',
  }