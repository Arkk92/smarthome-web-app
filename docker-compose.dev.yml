version: "3.8"
services:
  backend:
    image: smart-home-project/backend
    build:
      context: ./backend
      dockerfile: Dockerfile.dev
    ports:
      - '3000:3000'
    volumes:
      - ./backend:/app